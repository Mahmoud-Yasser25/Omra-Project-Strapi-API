'use strict';

/**
 * supermarket-order controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::supermarket-order.supermarket-order');
